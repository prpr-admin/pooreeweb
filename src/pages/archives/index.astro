---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const allItems = await getCollection('archives');
const items = allItems.sort((a, b) => (b.data.date?.valueOf() ?? 0) - (a.data.date?.valueOf() ?? 0));
const rendered = await Promise.all(items.map((p) => p.render()));
---

<BaseLayout title="Archives — 풀이">
  <div class="page">
    {items.map((item, i) => {
      const { Content } = rendered[i];
      return (
        <article class="entry">
          <div class="col-left">
            <h2 class="entry-title">{item.data.title}</h2>
            {item.data.date && (
              <p class="entry-date">
                {item.data.date.toLocaleDateString('ko-KR', { year: 'numeric', month: '2-digit', day: '2-digit' })}
              </p>
            )}
          </div>
          <div class="col-right">
            {item.data.description && (
              <p class="entry-desc">{item.data.description}</p>
            )}
            <div class="entry-body">
              <Content />
            </div>
            {item.data.images && item.data.images.length > 0 && item.data.images.map((img) => (
              <div class="gallery-item">
                <img src={img} alt="" class="gallery-img" loading="lazy" />
              </div>
            ))}
          </div>
        </article>
      );
    })}
  </div>
</BaseLayout>

<style>
  .page { width: 100%; }

  .entry {
    display: grid;
    grid-template-columns: 1fr 2fr;
    align-items: start;
    padding: 3rem 1.5rem;
    border-bottom: 0.5px solid var(--color-border);
  }

  .entry:last-child { border-bottom: none; }

  /* 좌측 */
  .col-left {
    padding-right: 2rem;
  }

  .entry-title {
    font-size: 13px;
    line-height: 1.9;
    letter-spacing: 0.01em;
    color: #1a1a1a;
    font-weight: 400;
  }

  .entry-date {
    font-size: 13px;
    line-height: 1.9;
    letter-spacing: 0.01em;
    color: #1a1a1a;
    font-weight: 400;
  }

  /* 우측 */
  .col-right {
    padding-left: 2rem;
  }

  .entry-desc {
    font-size: 13px;
    line-height: 1.9;
    letter-spacing: 0.01em;
    color: #1a1a1a;
    font-weight: 400;
    margin-bottom: 1.2em;
  }

  .entry-body {
    font-size: 13px;
    line-height: 1.9;
    letter-spacing: 0.01em;
    color: #1a1a1a;
    font-weight: 400;
  }

  .entry-body :global(p) { margin-bottom: 1.2em; }

  .entry-body :global(h2) {
    font-size: 13px;
    letter-spacing: 0.01em;
    font-weight: 500;
    margin: 1.5em 0 0.5em;
    color: #1a1a1a;
    text-transform: none;
  }

  .entry-body :global(ul),
  .entry-body :global(ol) {
    padding-left: 1.2em;
    margin-bottom: 1.2em;
  }

  .entry-body :global(li) {
    font-size: 13px;
    line-height: 1.9;
    letter-spacing: 0.01em;
    color: #1a1a1a;
  }

  .gallery-item {
    margin-top: 1.5rem;
  }

  .gallery-img {
    width: 100%;
    display: block;
  }

  @media (max-width: 768px) {
    .entry { grid-template-columns: 1fr; padding: 2rem 1rem; }
    .col-left { padding-right: 0; margin-bottom: 1.5rem; }
    .col-right { padding-left: 0; }
  }
</style>
